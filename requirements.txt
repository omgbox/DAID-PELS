# BookBot Dependencies
# Install: pip install -r requirements.txt
# Note: Pynini requires conda install -c conda-forge pynini
# Note: PyTorch requires Microsoft Visual C++ Redistributable
#       Download: https://aka.ms/vs/17/release/vc_redist.x64.exe

# NLP - Core
nltk==3.8.1                    # POS tagging, NER, chunking, WordNet
pysbd==0.3.4                   # Sentence boundary detection

# Search and Ranking
rank-bm25==0.2.2               # BM25Okapi implementation

# Neural (for MiniGPT, Word2Vec, MultiDimScorer, DistilGPT2)
torch                          # PyTorch (CPU-only)
numpy                          # Numerical computing
transformers>=4.30.0           # Hugging Face Transformers (for DistilGPT2)

# General Knowledge (for conversational chatbot)
wikipedia-api>=0.6.0           # Wikipedia access for open-domain questions

# Database
# SQLite3 is built into Python standard library (no pip install needed)

# Utilities
inflect==7.0.0                 # Singularization/pluralization

# Phonetic Matching
pronouncing                    # CMU Pronouncing Dictionary access (134K words)
jellyfish                      # Jaro-Winkler similarity, Soundex, Metaphone
abydos                         # Double Metaphone, phonetic algorithms

# FST Engine (install separately via conda)
# pynini==2.1.7                # Weighted FST library (OpenFst backend)
#                               # Install: conda install -c conda-forge pynini
